box: wercker/nodejs
build:
  steps:
    - npm-install
    - npm-test
deploy:
  steps:
    - heroku-deploy
  after-steps:
    - wantedly/pretty-slack-notify:
        team: post-intenet
        token: $SLACK_API_TOKEN
        channel: general
