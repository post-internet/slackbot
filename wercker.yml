box: wercker/nodejs
build:
  steps:
    - npm-install
    - npm-test
deploy:
  steps:
    - heroku-deploy
  after-steps:
    - sherzberg/slack-notify:
        subdomain: meiji
        token: $SLACK_TOKEN
        channel: "#general"
        username: wercker
        icon_url: https://avatars3.githubusercontent.com/u/1695193?s=140
